export interface SocialLoginCredential {
  provider: string;
  providerId: string;
}

export function SocialLoginCredential(params: Partial<SocialLoginCredential>) {
  return {

  } as SocialLoginCredential;
}