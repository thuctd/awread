import { notReadyDirective } from './not-ready.directive';

describe('notReadyDirective', () => {
  it('should create an instance', () => {
    const directive = new notReadyDirective();
    expect(directive).toBeTruthy();
  });
});
