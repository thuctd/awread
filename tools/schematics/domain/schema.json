{
  "$schema": "http://json-schema.org/schema",
  "id": "domain",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "name of global folder",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "directory"
  ]
}
