{
  "$schema": "http://json-schema.org/schema",
  "id": "feature-page",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "feature page name, like ui, example: register or login",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "directory": {
      "type": "string",
      "description": "directory name to add the page to, example: --directory=writer/web"
    },
    "feature": {
      "type": "string",
      "description": "feature name to add the page to, example: --feature=auth"
    }
  },
  "required": [
    "name",
    "directory",
    "feature"
  ]
}
